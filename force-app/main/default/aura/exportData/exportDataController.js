({
    myAction : function(component, event, helper) {

    },
    downloadCSV : function(component, event, helper){
        
       /*   var jsonData = [];
          var indexSubmittedBy, indexUniqueNum;

          // Find the indexes of "Submitted By" and "uniqueNum" fields
          for (var i = 0; i < jsonData.length; i++) {
            if (jsonData[i].Name === "Submitted By") {
              indexSubmittedBy = i;
            } else if (jsonData[i].Name === "uniqueNum") {
              indexUniqueNum = i;
            }
          }
          
          // Swap the positions of the fields
          var temp = jsonData[indexSubmittedBy];
          jsonData[indexSubmittedBy] = jsonData[indexUniqueNum];
          jsonData[indexUniqueNum] = temp; 

          console.log('json:'+JSON.stringify(temp));
          console.log('json:'+JSON.stringify(jsonData));*/
          // Original JSON
        }

    })